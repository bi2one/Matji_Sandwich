<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_user_main"
    style="@style/DefaultFill">
  
  <LinearLayout
      android:id="@+id/user_talk_wrapper"
      android:layout_width="fill_parent"
      android:layout_height="fill_parent"
      android:background="@drawable/bg_01"
      android:orientation="vertical">

    <!-- Main Title -->
    <com.matji.sandwich.widget.title.UserTitle
	android:id="@id/Titlebar"
	style="@style/DefaultWrap" />
    
    <!-- User Tab -->
    <com.matji.sandwich.widget.RoundTabHost
	android:id="@android:id/tabhost"
	android:layout_width="fill_parent"
	android:layout_height="fill_parent">
      <LinearLayout
	  android:orientation="vertical"
	  android:layout_width="fill_parent"
	  android:layout_height="fill_parent">
	<FrameLayout
	    android:id="@android:id/tabcontent"
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:layout_weight="1" />
	<TabWidget
	    android:id="@android:id/tabs"
	    style="@style/post_tab_TabBar"
	    android:background="@drawable/tabbtn_bg_under"
	    android:layout_weight="0" />
      </LinearLayout>
    </com.matji.sandwich.widget.RoundTabHost>

    <!-- Seperate Line -->
    <!-- <RelativeLayout -->
    <!-- style="@style/DefaultWidthFill"> -->
    <!-- <ImageView -->
    <!-- style="@style/DefaultWidthFill" -->
    <!-- android:background="@drawable/seperate_line" /> -->
    <!-- </RelativeLayout> -->
  </LinearLayout>
</RelativeLayout>