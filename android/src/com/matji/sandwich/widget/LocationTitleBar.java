package com.matji.sandwich.widget;


public class LocationTitleBar {
    // private Context context;
    // private TextView addressText;
    // private View currentPositionButton;
    // private View addressArrow;
    // private View moveToMapViewButton;

    // public LocationTitleBar(Context context) {
    // 	super(context);
    // 	init(context);
    // }

    // public LocationTitleBar(Context context, AttributeSet attrs) {
    // 	super(context, attrs);
    // 	init(context);
    // }

    // private void init(Context context) {
    // 	this.context = context;
    // 	LayoutInflater.from(context).inflate(R.layout.location_title_bar, this, true);
    // 	addressText = (TextView) findViewById(R.id.location_title_bar_address);
    // 	currentPositionButton = findViewById(R.id.location_title_bar_location_button);
    // 	addressArrow = findViewById(R.id.location_title_bar_address_arrow);
    // }

    // public void start(Context context) {
    // 	if (this.getParent() == null){
    // 		((Activity) context).addContentView(this, new LayoutParams(LayoutParams.FILL_PARENT, LayoutParams.FILL_PARENT));
    // 	}
    // 	setVisibility(View.VISIBLE);
    // }
    
    // public void stop() {
    // 	ViewGroup vGroup = (ViewGroup)this.getParent();
    // 	if (vGroup != null){
    //     	vGroup.removeView(this);
    // 	}
    // }
}